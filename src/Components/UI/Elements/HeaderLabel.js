import styled from "styled-components";
const HeaderLabel = styled.label`
color: ${props => props.theme.color};
font-weight: 500;
 margin: 0.2rem 0 0.4rem 0.2rem !important;
 font-size: 0.75rem;
 display:flex;
`;
export default HeaderLabel;
